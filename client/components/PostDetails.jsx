// Single post component
import React from 'react'

export default function PostDetails() {
    return (
        <>
            <h3></h3>
            <p></p>
        </>
    )
}


/*
import React from 'react'

function Post (props) {
  return (
    <>
      <h3>{props.title}</h3>
      <p><i>{props.date} | Comments: {props.commentCount}</i></p>
      {props.paragraphs.map((line, index) => {
        return (
          <p key={index}>{line}</p>
        )
      })}
    </>
  )
}

export default Post


*/